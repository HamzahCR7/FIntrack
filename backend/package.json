{
  "name": "fintrack-backend",
  "version": "1.0.0",
  "description": "FinTrack - AI-Powered Personal Finance Tracker Backend (Phase 1)",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "build:app": "npm --prefix ../frontend run build && npm run build",
    "start": "node dist/main.js",
    "start:app": "npm run build:app && npm start",
    "dev": "ts-node-dev --respawn --transpile-only src/main.ts",
    "prisma:generate": "prisma generate",
    "prisma:db:push": "prisma db push",
    "seed": "ts-node prisma/seed.ts",
    "test": "set \"DATABASE_URL=file:./test.db\"&& jest --runInBand",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "@prisma/client": "^5.10.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "pdf-parse": "^2.4.5",
    "sharp": "^0.35.4",
    "tesseract.js": "^7.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.24",
    "@types/supertest": "^6.0.2",
    "jest": "^29.7.0",
    "prisma": "^5.10.0",
    "supertest": "^6.3.4",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
